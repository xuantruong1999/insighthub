apiVersion: v1
kind: ServiceAccount
metadata:
  name: mcp-readonly
  namespace: insighthub
  labels:
    app.kubernetes.io/part-of: insighthub
    app.kubernetes.io/managed-by: trainer
  annotations:
    description: "ServiceAccount read-only cho MCP server — Day 2 InsightHub lab"
